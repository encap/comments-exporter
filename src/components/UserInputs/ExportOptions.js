const ExportOptions = () => {
  // strictMode - sets client-side filtering
  // matches whole searchTerm case insesitive
  return (
    <div>
      <h3>Export options</h3>
    </div>
  );
};

export default ExportOptions;
